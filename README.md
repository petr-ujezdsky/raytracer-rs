# raytracer-rs
Raytracer in Rust for educational purposes

## Build and run
You need to have `cargo` installed. To build the project execute
```bash
./build.sh
```

To run the project execute
```bash
./run.sh
```
